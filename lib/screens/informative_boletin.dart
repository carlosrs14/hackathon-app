import 'package:flutter/material.dart';

class InformativeBoletin extends StatelessWidget {
  const InformativeBoletin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("hola");
  }
}